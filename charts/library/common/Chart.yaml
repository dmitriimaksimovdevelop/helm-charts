---
apiVersion: v2
name: common
description: Function library for Helm charts
type: library
version: 4.3.0
kubeVersion: ">=1.28.0-0"
keywords:
  - common
  - library
home: https://github.com/bjw-s-labs/helm-charts/tree/main/charts/library/common
maintainers:
  - name: bjw-s
    email: me@bjw-s.dev
sources:
  - https://github.com/bjw-s-labs/helm-charts
annotations:
  artifacthub.io/changes: |-
    - kind: fixed
      description: |-
        defaultPodOptions no longer get accidentally overwritten when there are multiple controllers.
    - kind: added
      description: |-
        Allow configuring Service trafficDistribution in Kubernetes >=v1.33.
    - kind: added
      description: |-
        Allow `headers`, `queryParams`, and `method` matchers in HTTPRoutes.
    - kind: added
      description: |-
        Allow templating in `image.repository` and `image.tag`.
